export default function SocialMediaLinks({ currentStep }): JSX.Element {
  if (currentStep === 9) {
    return (
      <div>
        <h1>SM</h1>
      </div>
    );
  } else {
    return <></>;
  }
}
