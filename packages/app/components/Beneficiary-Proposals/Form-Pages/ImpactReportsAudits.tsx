export default function ImpactReportsAudit({ currentStep }): JSX.Element {
  if (currentStep === 8) {
    return (
      <div>
        <h1>IRA</h1>
      </div>
    );
  } else {
    return <></>;
  }
}
