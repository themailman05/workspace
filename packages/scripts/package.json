{
  "name": "@popcorn/scripts",
  "version": "1.0.0",
  "author": "Popcorn",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean-dist": "rimraf dist/*",
    "clean": "yarn rimraf dist",
    "build": "yarn clean-dist && tsc -b",
    "tsc": "tsc -b"
  }
}
